## 1 append() :-add an element to the end of the list.
# example:-
family = ['selam','mahi','tesfsh'] # a variable is specified in a list
family.append("ale") # a list method called append is used 
print(family) # ale is also be printed in the list. 

## 2 clear():- remove all the elements from the list.
# example:-
clothes = ['tshirt', 'pants', 'sweater'] # a variable is specified in a list 
clothes.clear() # a list method called clear is used 
print(clothes) # [] is expected to be printed

## 3 insert():- inserts an element at a specified index
# example:-
phone = ['samsung',iphone',xiaomi'] # a variable is specified in a list
phone.insert(1,'pixle') # a list method called insert is used.
print(phone) # pixle is also be listed in the list in the second index

## 4 len():- is a list metod that return tehe length of the list
# example:- 
proffesion ['teacher', 'doctor'] # a variable is specified in a list
subset = len(proffesion) # a list method called len is used
print(subset) #this will print "2"

## 5 pop():- is a list method that Removes the element at the given position in the list, or
the last element if no index is specified.
# example:-
app = ['telebirr', 'mpesa', dls25'] # a variable is specified in a list
app.sort() # list metod called pop is used
print(sort) # the last element in the list will be removed

## 6 remove() :- Removes the first occurrence of a value.
# example:-
browser = ['chrome'. 'phoneix', 'opera'] # variable is specified in a list
browser.remove("opera") # a list method called remove is used
print(browser) # "opera" is expected to be removed from the list 

## 7 slicing() :- Accessing sub-parts of a list.
# example:-
list method ['append', 'clear', 'pop', 'insert'] # variable is specified in a list
subset = list method[1:3] #  a list method called slicing is used
print(subset) # this will print ['pop']

## 8 sorting() :- Sorts the list in ascending order.
# example:-
number = ['2', '1', '4', '3'] #  variable is specified in a list
number.sort() # a list method called sorting is used
print(number) # this will print the list by sorting it. .
